/* eslint-disable no-shadow */

export enum AuthTypes {
  INIT = 'INIT',
  LOGIN = 'LOGIN',
  REGISTER = 'REGISTER',
  PERSON = 'PERSON',
  ERROR_PASS = 'ERROR_PASS',
}

export enum UiConfigTypes {
  SET_MODAL = 'SET_MODAL',
}

export enum Modals {
  FILM = 'FILM',
}
